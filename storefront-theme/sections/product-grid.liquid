<section id="{{ section.settings.anchor | default: 'shop' }}" class="sl-shop sl-wrap">
  <div class="sl-shop__head sl-reveal">
    <p class="sl-shop__eyebrow">{{ 'homepage.plans.eyebrow' | t }}</p>
    <h2 class="sl-h2">{{ section.settings.heading | default: 'Shop CaddieInsight' }}</h2>
    {%- if section.settings.subheading != blank -%}
      <p class="sl-shop__sub">{{ section.settings.subheading }}</p>
    {%- endif -%}
  </div>

  <div class="sl-shop__grid sl-reveal sl-reveal--d1">
    {%- for block in section.blocks -%}
      {%- liquid
        assign b = block.settings
        assign title = b.title
        assign price = b.price_text
        assign compare_at = ''
        assign url = b.url

        # If a real product is linked, let its data win over placeholder text.
        if b.product != blank
          assign p = all_products[b.product]
          if title == blank
            assign title = p.title
          endif
          if url == blank
            assign url = p.url
          endif
          if b.use_product_price
            if p.price_varies
              assign price = p.price_min | money | prepend: 'From '
            else
              assign price = p.price | money
              if p.compare_at_price > p.price
                assign compare_at = p.compare_at_price | money
              endif
            endif
          endif
        endif
        assign url = url | default: '#'
      -%}
      <a href="{{ url }}" class="sl-card{% if b.featured %} sl-card--featured{% endif %}"{{ block.shopify_attributes }}>
        <div class="sl-card__media">
          {% render 'media-placeholder',
            image: b.image,
            ratio_w: 400, ratio_h: 260,
            label: b.placeholder_label,
            warm: b.warm_placeholder,
            flush: true,
            alt: title %}
        </div>
        <div class="sl-card__body">
          {%- if b.badge != blank -%}<p class="sl-card__badge">{{ b.badge }}</p>{%- endif -%}
          <h3 class="sl-card__title">{{ title | default: 'Product' }}</h3>
          {%- if b.description != blank -%}<p class="sl-card__desc">{{ b.description }}</p>{%- endif -%}
          {%- if price != blank -%}
            <p class="sl-card__price">
              {{ price }}
              {%- if compare_at != blank %}
                <s class="sl-price-compare">{{ compare_at }}</s>
              {%- endif -%}
            </p>
          {%- endif -%}
          {%- if b.price_note != blank -%}<p class="sl-card__note">{{ b.price_note }}</p>{%- endif -%}
        </div>
      </a>
    {%- endfor -%}
  </div>
</section>

{% stylesheet %}
.sl-shop {
  padding-top: clamp(78px, 9vw, 126px);
  padding-bottom: clamp(78px, 9vw, 126px);
}
.sl-shop__head {
  max-width: 850px;
}
.sl-shop__eyebrow {
  margin: 0 0 18px;
  color: var(--sl-orange);
  font-family: var(--sl-font-mono);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.18em;
}
.sl-shop .sl-h2 {
  max-width: 13ch;
  font-size: clamp(40px, 5.2vw, 68px);
  font-weight: 900;
  letter-spacing: -0.05em;
  line-height: 0.98;
  text-wrap: balance;
}
.sl-shop__sub {
  max-width: 62ch;
  margin: 20px 0 0;
  font-size: 17px;
  line-height: 1.6;
  color: var(--sl-ink-soft);
}
.sl-shop__grid {
  display: grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(300px, 0.8fr);
  gap: 20px;
  margin-top: clamp(34px, 5vw, 56px);
}
.sl-card {
  text-decoration: none;
  color: inherit;
  background: var(--sl-bg-card);
  border: 1px solid var(--sl-border);
  border-radius: var(--sl-radius-lg);
  overflow: hidden;
  display: flex;
  flex-direction: column;
  box-shadow: var(--sl-shadow-card);
}
.sl-card:hover { border-color: rgba(20, 71, 44, 0.35); }
.sl-card--featured {
  position: relative;
  border-color: rgba(245, 242, 233, 0.14);
  background:
    radial-gradient(circle at 82% 6%, rgba(240, 122, 24, 0.2), transparent 30%),
    #0a1e14;
  color: #f5f2e9;
  box-shadow: 0 34px 76px -38px rgba(7, 19, 13, 0.82);
}
.sl-card--featured:hover { border-color: rgba(240, 122, 24, 0.62); }
.sl-card--featured .sl-card__media {
  background: rgba(255, 255, 255, 0.035);
  border-bottom: 1px solid rgba(245, 242, 233, 0.1);
}
.sl-card--featured .sl-card__media img { mix-blend-mode: screen; }
.sl-card--featured .sl-card__body { padding: clamp(22px, 3vw, 34px); }
.sl-card--featured .sl-card__badge { color: #ff9a42; }
.sl-card--featured .sl-card__title { color: #f5f2e9; font-size: clamp(25px, 3vw, 36px); }
.sl-card--featured .sl-card__desc { color: rgba(245, 242, 233, 0.68); font-size: 16px; }
.sl-card--featured .sl-card__price { color: #ffffff; font-size: 22px; }
.sl-card--featured .sl-card__note { color: rgba(245, 242, 233, 0.52); }
.sl-card__media { line-height: 0; }
.sl-card__media img,
.sl-card__media .sl-placeholder { display: block; width: 100%; }
.sl-card__body {
  padding: 18px 20px 22px;
  display: flex;
  flex-direction: column;
  gap: 6px;
  flex: 1;
}
.sl-card__badge {
  margin: 0;
  font-family: var(--sl-font-mono);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--sl-orange);
}
.sl-card__title {
  margin: 0;
  font-size: 20px;
  font-weight: 800;
  letter-spacing: -0.01em;
  color: var(--sl-green);
}
.sl-card__desc {
  margin: 0;
  font-size: 14.5px;
  line-height: 1.5;
  color: var(--sl-ink-soft);
  flex: 1;
}
.sl-card__price {
  margin: 6px 0 0;
  font-size: 16px;
  font-weight: 700;
  font-variant-numeric: tabular-nums;
}
.sl-card__note {
  margin: 0;
  font-size: 13px;
  color: var(--sl-ink-muted);
}
@media (prefers-reduced-motion: no-preference) {
  .sl-card { transition: transform .22s ease, box-shadow .22s ease, border-color .22s ease; }
  .sl-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--sl-shadow-card-hover);
  }
}
@media (max-width: 749px) {
  .sl-shop__grid { grid-template-columns: 1fr; }
  .sl-shop .sl-h2 { font-size: clamp(40px, 12vw, 56px); }
}
{% endstylesheet %}

{% schema %}
{
  "name": "Product grid",
  "settings": [
    { "type": "text", "id": "anchor", "label": "Section anchor id", "default": "shop" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Start with CaddieInsight" },
    { "type": "text", "id": "subheading", "label": "Subheading", "default": "Buy Pro here, then open CaddieInsight to use it" }
  ],
  "blocks": [
    {
      "type": "product_card",
      "name": "Product card",
      "settings": [
        { "type": "text", "id": "badge", "label": "Badge", "default": "MEMBERSHIP" },
        { "type": "text", "id": "title", "label": "Title", "default": "CaddieInsight Pro" },
        { "type": "textarea", "id": "description", "label": "Description", "default": "Unlimited swing analyses with every deliverable — strip, overlay, slow motion, notes." },
        { "type": "text", "id": "price_text", "label": "Price text", "info": "Leave blank when using the linked product's live price." },
        { "type": "text", "id": "price_note", "label": "Price note", "info": "Small line under the price, e.g. plan lengths." },
        { "type": "url", "id": "url", "label": "Card link" },
        { "type": "product", "id": "product", "label": "Linked product (optional)", "info": "Link a real product to pull its title, URL and price." },
        { "type": "checkbox", "id": "use_product_price", "label": "Use the linked product's live price", "default": false },
        { "type": "image_picker", "id": "image", "label": "Card image", "info": "Leave blank to show the striped placeholder." },
        { "type": "text", "id": "placeholder_label", "label": "Placeholder caption", "default": "product art" },
        { "type": "checkbox", "id": "warm_placeholder", "label": "Warm (tan) placeholder stripes", "default": false },
        { "type": "checkbox", "id": "featured", "label": "Feature this plan", "default": false }
      ]
    }
  ],
  "max_blocks": 6,
  "presets": [
    {
      "name": "Product grid",
      "blocks": [
        {
          "type": "product_card",
          "settings": {
            "badge": "MEMBERSHIP",
            "title": "CaddieInsight Pro",
            "description": "Unlimited swing analyses with every deliverable — strip, overlay, slow motion, notes.",
            "use_product_price": true,
            "placeholder_label": "pro membership art",
            "warm_placeholder": false
          }
        },
        {
          "type": "product_card",
          "settings": {
            "badge": "FREE PLAN",
            "title": "Try CaddieInsight Free",
            "description": "1 full swing analysis every month, free forever — the complete report each time.",
            "price_text": "$0",
            "price_note": "No card required",
            "placeholder_label": "free plan report strip",
            "warm_placeholder": true
          }
        }
      ]
    }
  ]
}
{% endschema %}
