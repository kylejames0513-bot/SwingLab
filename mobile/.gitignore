# Expo / React Native build and dependency output
node_modules/
.expo/
dist/
web-build/
*.tsbuildinfo

# Native projects are generated by `expo prebuild` from app.json
ios/
android/

# Local environment
.env
.env.*
!.env.example
