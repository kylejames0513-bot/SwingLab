__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Local environment files may contain credentials; commit names/examples only.
.env
.env.*
!.env.example

# downloaded pose model (fetched and cached on first run)
swinglab/models/

# analysis output
results/

# web app sessions (uploads, deliverables, job database)
sessions/

# operator-created backup and scratch-restore output must stay outside the repo
/backup-output/
/restore-scratch/

# store-asset fonts (fetched with the commands in store-assets/README.md)
store-assets/*.ttf

# Local Superpowers brainstorming screens and session state.
/.superpowers/

# Isolated implementation worktrees.
/.worktrees/

# Expo mobile client (mobile/)
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/expo-env.d.ts
mobile/ios/
mobile/android/
mobile/coverage/
mobile/*.jks
mobile/*.p8
mobile/*.p12
mobile/*.mobileprovision
mobile/.kotlin/
