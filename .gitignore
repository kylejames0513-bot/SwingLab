__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# local environment files may contain production credentials
.env
.env.*
!.env.example

# downloaded pose model (fetched and cached on first run)
swinglab/models/

# analysis output
results/

# web app sessions (uploads, deliverables, job database)
sessions/

# store-asset fonts (fetched with the commands in store-assets/README.md)
store-assets/*.ttf
